/**
 * task.js
 * task constants
 */

const prefix = 'task_'

export default {
  actions: {
    listLoaded: prefix + 'list_loaded',
    added: prefix + 'added',
    updated: prefix + 'updated',
    deleted: prefix + 'deleted',
    failed: prefix + 'failed'
  }
}
